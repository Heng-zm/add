name: chat
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1 # Or latest
  shared_preferences: ^2.2.3 # Or latest
  url_launcher: ^6.3.0 # Or latest
  share_plus: ^9.0.0 # Or latest
  image_picker: ^1.1.2 # Or latest
  provider: ^6.1.2 # Recommended for theme/settings state
  # Potentially add:
  # mobile_scanner: ^5.1.1 # Or camera plugin
  # qr_flutter: ^4.1.0
  # contacts_service: ^0.6.3 # Or flutter_contacts
  # add_2_calendar: ^3.0.0
  # vibration: ^1.8.4
  # audioplayers: ^6.0.0
  # wifi_connector: ^0.1.2+1 # Or platform channels for Wi-Fi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
